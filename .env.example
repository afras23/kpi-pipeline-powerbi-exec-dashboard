# Copy to .env and adjust for your environment.
# SQLite (local dev)
DATABASE_URL=sqlite:///./data/processed/kpi_mart.db

# Postgres (production / docker-compose)
# DATABASE_URL=postgresql://kpi:kpi@localhost:5432/kpi

RAW_DATA_DIR=data/raw
ENV=development
RUN_ETL_ON_STARTUP=false
